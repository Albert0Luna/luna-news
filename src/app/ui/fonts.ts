import {Outfit, Oxygen } from 'next/font/google'

export const outfit = Outfit({
  subsets: ['latin'],
  display: 'swap',
})

export const oxygen = Oxygen({
  weight: ["700"],
  subsets: ['latin'],
  display: 'swap',
})
