/*import {fetchNews} from '@/src/app/lib/data';

export default async function Page () {
  const news = await fetchNews();
  return (
    <main>
      <h1>Prueba de testing</h1>

      <ul>
        {news.map((item, index) => (
          <li key={index}>{item.title}</li>
        ))}
      </ul>
    </main>
  );
}
*/

